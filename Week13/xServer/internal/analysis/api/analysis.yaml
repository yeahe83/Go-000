type: google.api.Service
config_version: 3

http:
  rules:
    - selector: analysis.v1.AnalysisService.GetYoY
      post: /v1/yoy
      body: "*"
    - selector: analysis.v1.AnalysisService.GetMoM
      post: /v1/mom
      body: "*"